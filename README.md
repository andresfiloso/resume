# Andrés Filoso – Resume

This repository contains my resume written in YAML and rendered using [RenderCV](https://github.com/rendercv/rendercv).

## Quick Start

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
rendercv render andresfiloso_resume.yaml
```